name: test-samples

on:
    pull_request:
    push:
        branches: [ develop, main ]

env:
    DOTNET_VERSION: '6.x'

jobs:
    run-samples:
        runs-on: ${{ matrix.os }}
        defaults:
          run:
            shell: bash
        strategy:
          matrix:
            os: [windows-latest, ubuntu-latest]
            dir: [
              'Annotations/Annotations/'
              ]
        steps:
            - name: Checkout
              uses: actions/checkout@v4
            - name: Setup .NET
              uses: actions/setup-dotnet@v4
              with:
                dotnet-version: ${{ env.DOTNET_VERSION }}
            - name: List files
              run: |
                ls ${{matrix.dir}}
            - name: Build samples
              working-directory: ${{ matrix.dir }}
              run: |
                dotnet build -c Release ${{ matrix.dir }}*.csproj
